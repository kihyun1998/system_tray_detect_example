name: system_tray_detect_example
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  cupertino_icons: ^1.0.8
  ffi: ^2.1.3
  flutter:
    sdk: flutter
  system_tray: ^2.0.3
  win32: ^5.10.0
  window_manager: ^0.4.3

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/app_icon.ico
